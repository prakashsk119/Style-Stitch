const mongoose =require('mongoose')
const adminschema= new mongoose.Schema({
    name:String,
    email:String,
    password:Number
})

const AdminModel =mongoose.model('/projects',adminschema)

module.exports=AdminModel;